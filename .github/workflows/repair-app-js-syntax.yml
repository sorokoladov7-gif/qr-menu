name: Repair app.js syntax

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Remove malformed Hall/notification block and validate
        shell: python
        run: |
          from pathlib import Path
          p = Path('js/app.js')
          s = p.read_text(encoding='utf-8')
          marker = '/* =========================================================\n   ADVANCED MANAGER HALL'
          if marker in s:
              s = s.split(marker, 1)[0].rstrip() + '\n'
              p.write_text(s, encoding='utf-8')
          else:
              print('Advanced Hall block already absent')
      - name: Syntax check
        run: node --check js/app.js
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add js/app.js
          if git diff --cached --quiet; then echo 'No changes'; exit 0; fi
          git commit -m 'fix: remove malformed Hall block from app.js'
          git push origin main
