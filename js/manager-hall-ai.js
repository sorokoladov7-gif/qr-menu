/* QR Menu — legacy compatibility stub.
   The manager hall is now owned by manager-hall-view.js + manager-hall-direct.js.
   This file intentionally does not bind click handlers, inspect Vue, show alerts,
   use localStorage, or create an iframe. It remains only because manager.html
   still references this legacy filename. */
(function(){
  'use strict';
  window.__QR_MANAGER_HALL_LEGACY_DISABLED__ = true;
})();
