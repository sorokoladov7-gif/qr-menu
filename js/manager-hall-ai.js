/* Compatibility stub.
 * The manager hall is now implemented by js/manager-hall-view.js directly
 * inside the manager cabinet. This file intentionally does nothing so an
 * older script reference cannot create a second iframe/venue context.
 */
(function(){'use strict';window.__managerHallAICompat=true;})();
