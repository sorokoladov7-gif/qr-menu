/* QRChick — intelligent recipe workspace extension. */
(function(){
  'use strict';
  if(window.__QR_RECEPT_AI_BOOTED__) return;
  window.__QR_RECEPT_AI_BOOTED__=true;
  var state=function(){return window.__QR_MANAGER_RECIPES_STATE__||{};};
  var root=function(){return document.querySelector('.recipe-tab-container[data-qr-sections="1"]')||document.querySelector('.recipe-tab-container');};
  var esc=function(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c];});};
  var norm=function(v){return String(v||'').toLowerCase().replace(/ё/g,'е').replace(/[^а-яa-z0-9]+/g,' ').trim().replace(/\s+/g,' ');};
  var GENERIC_ERROR='Ошибка системы. Обратитесь к администратору платформы.';
  function status(text,error){var a=document.getElementById('qrReceptAiStatus'),b=document.getElementById('ocrStatus');if(a){a.textContent=text||'';a.style.color=error?'#fca5a5':'';}if(b){b.textContent=text||'';if(error)b.style.color='#fca5a5';}}
  function fail(el){if(el)el.innerHTML='<div style="color:#fca5a5">'+GENERIC_ERROR+'</div>';status(GENERIC_ERROR,true);}
  function rpc(name,args){if(!window.db||!window.db.rpc)return Promise.reject(new Error('DB'));return window.db.rpc(name,args).then(function(r){if(r.error)throw r.error;return r.data;});}
  function token(){return window.db.auth.getSession().then(function(r){if(r.error)throw r.error;var t=r.data&&r.data.session&&r.data.session.access_token;if(!t)throw new Error('AUTH');return t;});}
  function ai(mode,message,context,image){return token().then(function(t){return fetch('/api/manager-ai-propose',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+t},body:JSON.stringify({feature:'recipes',mode:mode,message:message||'',context:context||'',image:image||null})});}).then(function(r){return r.json().catch(function(){return{};}).then(function(d){if(!r.ok||d.ok===false)throw new Error('AI');return d;});});}
  function result(d){if(d&&d.analysis&&typeof d.analysis==='object')return d.analysis;if(d&&d.result&&typeof d.result==='object')return d.result;if(d&&d.answer&&typeof d.answer==='object')return d.answer;var t=String(d&&d.answer||'').trim().replace(/^```json/i,'').replace(/```$/,'').trim();try{return JSON.parse(t);}catch(e){var a=t.indexOf('{'),b=t.lastIndexOf('}');if(a>=0&&b>a)try{return JSON.parse(t.slice(a,b+1));}catch(_){}return{};}}
  function products(){return(state().products||[]).filter(function(x){return x&&x.id&&x.name;});}
  function product(id){return products().find(function(x){return String(x.id)===String(id);})||null;}
  function localIngredient(name){var n=norm(name);if(!n)return null;return(state().ingredients||[]).find(function(x){var z=norm(x.name);return z===n||z.indexOf(n)>=0||n.indexOf(z)>=0;})||null;}
  function catalog(names,limit){var s=state();if(!s.venueId||!names||!names.length)return Promise.resolve([]);return rpc('manager_recipe_catalog_for_venue',{p_venue_id:s.venueId,p_product_names:names,p_limit:limit||100});}
  function ingredientCatalog(names,limit){var s=state();if(!s.venueId||!names||!names.length)return Promise.resolve([]);return rpc('manager_ingredient_catalog_for_venue',{p_venue_id:s.venueId,p_product_names:names,p_limit:limit||350});}
  function refreshIngredients(){return rpc('manager_ingredient_list',{p_venue_id:state().venueId}).then(function(x){state().ingredients=Array.isArray(x)?x:[];return state().ingredients;});}
  function steps(v){v=Array.isArray(v)?v:[];if(!v.length)return '<div class="muted">Технология не заполнена.</div>';return '<ol style="margin:6px 0 0 20px">'+v.map(function(x){var t=typeof x==='string'?x:(x&&((x.text_ru)||x.text||(x.description)))||'';return '<li>'+esc(t)+(x&&x.minutes!=null?' <span class="muted">('+esc(x.minutes)+' мин)</span>':'')+'</li>';}).join('')+'</ol>';}
  function panel(title,sub){return '<div class="qrchick-panel" style="margin:0 0 14px;padding:13px;border:1px solid rgba(99,102,241,.3);border-radius:14px;background:rgba(99,102,241,.08)"><div style="display:flex;gap:10px;align-items:center"><img src="/assets/img/qrchick-avatar.svg" alt="QRChick" style="width:46px;height:46px;border-radius:50%"><div><b>QRChick</b><div class="muted" style="font-size:11px">'+esc(title)+'</div><div class="muted" style="font-size:10px">'+esc(sub)+'</div></div></div><div class="qrchick-body" style="margin-top:10px"></div></div>';}
  function ensurePanel(sec,title,sub){if(!sec||!state().baseLoaded)return null;var p=sec.querySelector('.qrchick-panel'),v=state().venueId||'none';if(!p){sec.insertAdjacentHTML('afterbegin',panel(title,sub));p=sec.querySelector('.qrchick-panel');}if(p.dataset.venue!==v){p.dataset.venue=v;p.dataset.ready='';}return p;}
  function matchProduct(p,out){if(!p||!out)return;out.innerHTML='<div class="muted">🔎 QRChick анализирует название, состав и кулинарный контекст…</div>';Promise.all([catalog([p.name],120),ingredientCatalog([p.name],350),refreshIngredients()]).then(function(x){var recipes=x[0]||[],ings=x[1]||[],local=x[2]||[];if(!recipes.length)throw new Error('NO_RECIPES');return ai('recipe_match','Сопоставь блюдо меню с рецептурой максимально точно. Учитывай синонимы, опечатки, состав, категорию, кухню, выход и ингредиенты. Проверь совместимость рецептуры с локальными ингредиентами. Верни только JSON {"selected_recipe_id":string|null,"confidence":number,"ingredient_matches":[{"recipe_ingredient_name":string,"local_ingredient_id":string|null,"confidence":number}],"reason":string}. Используй только ID из CANDIDATES.',JSON.stringify({product:p,candidates:recipes,ingredient_candidates:ings,local_ingredients:local})).then(function(d){var r=result(d),id=r.selected_recipe_id||null,rr=id&&recipes.find(function(q){return String(q.recipe_id)===String(id);});if(!rr||Number(r.confidence||0)<0.55){out.innerHTML='<div class="muted">Уверенного совпадения не найдено.</div>';return;}renderRecipe(p,rr,r,out);});}).catch(function(e){console.error('[QRChick][recipe-match]',e);fail(out);});}
  function renderRecipe(p,rr,match,out){var arr=(rr.ingredients||[]).map(function(i){return{item:i,checked:true};});out.innerHTML='<div class="qrchick-result"><div style="display:flex;justify-content:space-between;gap:8px;flex-wrap:wrap"><div><b>'+esc(rr.recipe_name)+'</b><div class="muted" style="font-size:10px">'+esc(rr.matched_product_name||p.name)+' · уверенность '+Math.round(Number(match.confidence||0)*100)+'%</div></div><button type="button" class="btn btn-primary btn-sm" data-qrc-save>💾 Сохранить рецептуру</button></div><div style="margin-top:8px;display:grid;gap:5px">'+arr.map(function(x,i){return'<label><input type="checkbox" data-qrc-r="'+i+'" checked> '+esc(x.item.name)+' — '+esc(x.item.quantity)+' '+esc(x.item.unit||'')+'</label>';}).join('')+'</div><div style="margin-top:8px">'+(rr.description?'<div style="font-size:11px">'+esc(rr.description)+'</div>':'')+steps(rr.steps)+'</div></div>';out.querySelectorAll('[data-qrc-r]').forEach(function(c){c.onchange=function(){arr[Number(c.dataset.qrcR)].checked=c.checked;};});var b=out.querySelector('[data-qrc-save]');if(b)b.onclick=function(){saveRecipe(p,arr,out);};}
  function saveRecipe(p,arr,out){var chosen=arr.filter(function(x){return x.checked;}).map(function(x){return x.item;});if(!chosen.length){status('Выберите ингредиенты.',true);return;}status('QRChick сохраняет рецептуру…');chosen.reduce(function(q,x){return q.then(function(){return localIngredient(x.name)?null:rpc('manager_ingredient_upsert',{p_venue_id:state().venueId,p_name:x.name,p_unit:x.unit||'g',p_purchase_quantity:1,p_purchase_price:0,p_id:null});});},Promise.resolve()).then(refreshIngredients).then(function(){var rows=chosen.map(function(x){var i=localIngredient(x.name);return i?{ingredient_id:i.id,quantity:Number(x.quantity)||1,note:'QRChick · внутренняя БД'}:null;}).filter(Boolean);return rpc('manager_product_recipe_save',{p_venue_id:state().venueId,p_product_id:p.id,p_rows:rows});}).then(function(){status('Рецептура сохранена.');if(out)out.innerHTML='<div class="qrchick-result"><b>✅ Рецептура сохранена</b><div class="muted">'+esc(p.name)+'</div></div>';window.dispatchEvent(new CustomEvent('qr-recipes-data-changed'));}).catch(function(e){console.error('[QRChick][save-recipe]',e);fail(out);});}
  function recipeBlock(){var r=root(),sec=r&&r.querySelector('[data-section="recipes"]');if(!sec)return;var p=ensurePanel(sec,'Интеллектуальное сопоставление рецептур','Анализируются блюдо, состав и ингредиенты внутренней базы.');if(!p)return;var b=p.querySelector('.qrchick-body');if(p.dataset.productCount!==String(products().length)||!b.dataset.bound){p.dataset.productCount=String(products().length);b.dataset.bound='1';b.innerHTML='<div style="display:grid;grid-template-columns:minmax(180px,.7fr) minmax(260px,1.3fr);gap:10px"><div style="display:grid;gap:6px" data-qrc-product-list>'+products().map(function(x){return'<button type="button" class="btn btn-ghost btn-sm" data-qrc-product="'+esc(x.id)+'" style="text-align:left">🍽️ '+esc(x.name)+'</button>';}).join('')+'</div><div id="qrcRecipeResult"><div class="muted">Выберите блюдо.</div></div></div>';b.querySelectorAll('[data-qrc-product]').forEach(function(x){x.onclick=function(){matchProduct(product(x.dataset.qrcProduct),b.querySelector('#qrcRecipeResult'));};});}}
  function searchTech(p,out){if(!p||!out)return;out.innerHTML='<div class="muted">🔎 QRChick сопоставляет техкарту, блюдо и ингредиенты…</div>';Promise.all([catalog([p.name],120),ingredientCatalog([p.name],350)]).then(function(x){var recipes=x[0]||[],ings=x[1]||[];if(!recipes.length)throw new Error('NO');return ai('recipe_match','Найди лучшую техкарту для блюда. Сопоставь одновременно название, состав и ингредиенты. Верни JSON {"selected_recipe_id":string|null,"confidence":number,"reason":string}. Используй только ID CANDIDATES.',JSON.stringify({product:p,candidates:recipes,ingredient_candidates:ings})).then(function(d){var r=result(d),rr=r.selected_recipe_id&&recipes.find(function(q){return String(q.recipe_id)===String(r.selected_recipe_id);});if(!rr||Number(r.confidence||0)<0.55){out.innerHTML='<div class="muted">Уверенного совпадения не найдено.</div>';return;}out.innerHTML='<div class="qrchick-result"><b>'+esc(rr.recipe_name)+'</b><div class="muted" style="font-size:10px">Уверенность '+Math.round(Number(r.confidence||0)*100)+'%</div><div style="margin-top:6px">'+esc(rr.description||'')+'</div>'+steps(rr.steps)+'<button type="button" class="btn btn-primary btn-sm" data-qrc-import>➕ Добавить техкарту</button></div>';var b=out.querySelector('[data-qrc-import]');b.onclick=function(){b.disabled=true;b.textContent='Сохранение…';rpc('manager_tech_card_import_global_recipe',{p_venue_id:state().venueId,p_product_id:p.id,p_recipe_id:rr.recipe_id}).then(function(){status('Техкарта добавлена.');state().techLoaded=false;return refreshTechCards();}).then(function(){techEnhance();}).catch(function(e){console.error('[QRChick][import-tech]',e);fail(out);b.disabled=false;b.textContent='➕ Добавить техкарту';});};});}).catch(function(e){console.error('[QRChick][tech-search]',e);fail(out);});}
  function catalogBlock(){var r=root(),sec=r&&r.querySelector('[data-section="catalog"]');if(!sec)return;var old=document.getElementById('catalogList');if(old)old.style.display='none';var p=ensurePanel(sec,'Интеллектуальный поиск техкарт','Поиск учитывает название блюда, состав, категорию и ингредиенты.');if(!p)return;if(p.dataset.ready==='1')return;p.dataset.ready='1';var b=p.querySelector('.qrchick-body');b.innerHTML=products().map(function(x){return'<div style="padding:8px;border:1px solid rgba(255,255,255,.06);border-radius:9px;margin-bottom:6px"><div style="display:flex;justify-content:space-between;gap:8px;align-items:center"><b>'+esc(x.name)+'</b><button type="button" class="btn btn-primary btn-sm" data-qrc-tech="'+esc(x.id)+'">🔎 Сопоставить</button></div><div data-qrc-tech-result style="margin-top:8px"></div></div>';}).join('');b.querySelectorAll('[data-qrc-tech]').forEach(function(btn){btn.onclick=function(){searchTech(product(btn.dataset.qrcTech),btn.parentElement.parentElement.querySelector('[data-qrc-tech-result]'));};});}
  function ingredientsBlock(){var r=root(),sec=r&&r.querySelector('[data-section="ingredients"]');if(!sec)return;var old=document.getElementById('ingredientsDbList');if(old)old.style.display='none';var p=ensurePanel(sec,'Интеллектуальное сопоставление ингредиентов','Ингредиенты сопоставляются с блюдами и рецептурами по смыслу, синонимам и кулинарному контексту.');if(!p)return;if(p.dataset.ready==='1')return;p.dataset.ready='1';var b=p.querySelector('.qrchick-body');b.innerHTML='<div class="muted">🔎 Анализируем меню и базу ингредиентов…</div>';Promise.all([ingredientCatalog(products().map(function(x){return x.name;}),350),refreshIngredients(),catalog(products().map(function(x){return x.name;}),120)]).then(function(x){var cands=x[0]||[],local=x[1]||[],recipes=x[2]||[];return ai('recipe_match','Сопоставь ингредиенты внутренней БД с блюдами и рецептурами. Верни JSON {"selected_ingredient_ids":string[],"ingredient_matches":[{"candidate_id":string,"confidence":number,"reason":string}],"confidence":number,"reason":string}. Используй только ID CANDIDATES.',JSON.stringify({menu:products(),recipes:recipes,candidates:cands,local_ingredients:local})).then(function(d){var r=result(d),ids=Array.isArray(r.selected_ingredient_ids)?r.selected_ingredient_ids:[],by={};cands.forEach(function(i){by[String(i.id)]=i;});var list=(ids.length?ids.map(function(id){return by[String(id)];}).filter(Boolean):cands.slice(0,150));if(!list.length){b.innerHTML='<div class="muted">Подходящих ингредиентов не найдено.</div>';return;}b.innerHTML=list.map(function(i){var l=localIngredient(i.name);return'<div style="display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:center;padding:9px;border:1px solid rgba(255,255,255,.07);border-radius:9px;margin-bottom:5px"><span><b>'+esc(i.name)+'</b><span class="muted"> · '+esc(i.unit||'')+'</span><small class="muted" style="display:block">'+esc(i.matched_recipe_name||i.category||'')+'</small></span><span style="display:flex;gap:6px"><button type="button" class="btn btn-primary btn-sm" data-qrc-add="'+esc(i.id)+'" '+(l?'disabled':'')+'>'+(l?'Добавлен':'➕ Добавить')+'</button></span></div>';}).join('');var map={};list.forEach(function(i){map[String(i.id)]=i;});b.querySelectorAll('[data-qrc-add]').forEach(function(btn){btn.onclick=function(){var i=map[String(btn.dataset.qrcAdd)];if(!i)return;btn.disabled=true;btn.textContent='Сохранение…';rpc('manager_ingredient_upsert',{p_venue_id:state().venueId,p_name:i.name,p_unit:i.unit||'g',p_purchase_quantity:1,p_purchase_price:0,p_id:null}).then(refreshIngredients).then(function(){btn.textContent='Добавлен';window.dispatchEvent(new CustomEvent('qr-recipes-data-changed'));}).catch(function(e){console.error('[QRChick][add-ingredient]',e);fail(b);});};});});}).catch(function(e){console.error('[QRChick][ingredients]',e);fail(b);});}
  function compress(file){return new Promise(function(resolve,reject){var fr=new FileReader();fr.onerror=reject;fr.onload=function(){var img=new Image();img.onerror=reject;img.onload=function(){var max=2200,s=Math.min(1,max/Math.max(img.width,img.height)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*s));c.height=Math.max(1,Math.round(img.height*s));var ctx=c.getContext('2d');ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality='high';ctx.drawImage(img,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',.9));};img.src=fr.result;};fr.readAsDataURL(file);});}
  function analyzePhoto(file){status('QRChick анализирует фотографию техкарты…');return compress(file).then(function(dataUrl){var m=dataUrl.match(/^data:([^;]+);base64,(.*)$/s);if(!m)throw new Error('IMAGE');return ai('recipe_image_analyze','Полностью проанализируй фотографию техкарты. Распознай весь видимый русский текст, таблицы, название блюда, ингредиенты и количества, выход, порции, технологию, температуры, оборудование, хранение, подачу, требования качества и аллергены. Ничего не выдумывай. Верни только recipe_analysis.',JSON.stringify({venue_id:state().venueId,menu:products().map(function(p){return{id:p.id,name:p.name,category:p.category};})}),{mime_type:m[1],data:m[2]});}).then(function(d){return{analysis:result(d),file:file};});}
  function matchPhoto(analysis){var name=analysis.recipe_name||'';var names=[name].concat((analysis.ingredients||[]).map(function(x){return x.name||'';})).filter(Boolean);var menu=products();return Promise.all([catalog(name?[name]:menu.map(function(x){return x.name;}),120),ingredientCatalog(names.length?names:menu.map(function(x){return x.name;}),350)]).then(function(x){var recipes=x[0]||[],ings=x[1]||[];return ai('recipe_match','Сопоставь результат анализа фотографии с меню, техкартами и ингредиентами внутренней БД. Выбери лучшее блюдо и рецептуру. Отдельно сопоставь каждый распознанный ингредиент с кандидатом. Верни JSON {"selected_product_id":string|null,"selected_recipe_id":string|null,"confidence":number,"ingredient_matches":[{"source_name":string,"candidate_id":string|null,"candidate_name":string|null,"confidence":number,"reason":string}],"reason":string}. Используй только реальные ID из CONTEXT.',JSON.stringify({photo_analysis:analysis,menu:menu,candidate_recipes:recipes,candidate_ingredients:ings})).then(function(d){return{analysis:analysis,match:result(d),recipes:recipes,ingredients:ings};});});}
  function showVision(pack){var data=pack.analysis||{},m=pack.match||{},p=document.getElementById('ocrPanel');if(p)p.hidden=false;var text=document.getElementById('ocrText');if(text)text.textContent=JSON.stringify({analysis:data,match:m},null,2);window.__QRCHICK_LAST_TECH__=data;var sel=document.getElementById('ocrProduct'),chosen=m.selected_product_id&&product(m.selected_product_id);if(sel)sel.innerHTML=products().map(function(x){return'<option value="'+esc(x.id)+'" '+(chosen&&String(chosen.id)===String(x.id)?'selected':'')+'>'+esc(x.name)+'</option>';}).join('');var st=document.getElementById('ocrStatus');if(st)st.textContent='Анализ и сопоставление завершены. Проверяйте результат перед сохранением.';var acts=document.getElementById('qrTechAiActions');if(!acts&&p){acts=document.createElement('div');acts.id='qrTechAiActions';acts.style.marginTop='10px';p.appendChild(acts);}if(acts)acts.innerHTML='<button type="button" class="btn btn-primary" id="qrSaveAiTech">💾 Сохранить техкарту и сопоставление</button>';var b=document.getElementById('qrSaveAiTech');if(b)b.onclick=function(){saveVision(sel&&sel.value?product(sel.value):chosen||products()[0],data,m,pack.file);};}
  function saveVision(p,data,match,file){if(!p){status('Выберите блюдо.',true);return;}status('Сохраняем техкарту и результаты сопоставления…');var payload=Object.assign({},data,{ai_match:match||{},matched_product_id:p.id});rpc('manager_tech_card_ai_create',{p_venue_id:state().venueId,p_product_id:p.id,p_file_name:file&&file.name||('Техкарта: '+(data.recipe_name||p.name)),p_ocr_text:JSON.stringify(payload,null,2),p_recipe_data:payload}).then(function(){status('Техкарта сохранена и сопоставлена.');state().techLoaded=false;return refreshTechCards();}).then(function(){techEnhance();}).catch(function(e){console.error('[QRChick][save-tech]',e);status(GENERIC_ERROR,true);});}
  function photoUpload(){var f=document.getElementById('techFiles');if(!f||f.__qrcVision)return;f.__qrcVision=true;f.onchange=function(){var files=Array.prototype.slice.call(f.files||[]);f.value='';if(!files.length)return;files.reduce(function(q,file){return q.then(function(){return analyzePhoto(file).then(matchPhoto).then(showVision);});},Promise.resolve()).catch(function(e){console.error('[QRChick][photo]',e);status(GENERIC_ERROR,true);});};}
  function refreshTechCards(){var s=state();if(!s.venueId)return Promise.resolve([]);return window.db.from('manager_tech_cards').select('id,product_id,file_name,file_path,file_url,ocr_text,status,created_at,title,source_type,global_recipe_id,recipe_data').eq('venue_id',s.venueId).order('created_at',{ascending:false}).then(function(r){if(r.error)throw r.error;s.techCards=r.data||[];s.techLoaded=true;var box=document.getElementById('techList');if(box){var cards=s.techCards||[];box.innerHTML=cards.length?cards.map(function(t){return'<div class="tech-card">'+(t.file_url?'<img src="'+esc(t.file_url)+'" alt="" onerror="this.style.display=\'none\'">':'')+'<b>'+esc(t.title||t.file_name||'Техкарта')+'</b><div class="muted">'+esc(t.status==='processed'?'Распознано':'Загружено')+'</div><div style="display:flex;gap:6px;flex-wrap:wrap;margin-top:6px"><button type="button" class="btn btn-ghost btn-sm" data-tech="'+esc(t.id)+'">Открыть</button><button type="button" class="btn btn-ghost btn-sm" data-qrc-edit-tech="'+esc(t.id)+'">✏️ Изменить</button></div></div>';}).join(''):'<div class="muted">Техкарт пока нет.</div>';box.querySelectorAll('[data-tech]').forEach(function(b){b.onclick=function(){var t=cards.find(function(x){return String(x.id)===String(b.dataset.tech);});var p=document.getElementById('ocrPanel');if(p)p.hidden=false;var txt=document.getElementById('ocrText');if(txt)txt.textContent=t&&t.ocr_text||'Текст не распознан';};}}return s.techCards;});}
  function editTech(t){if(!t)return;var title=prompt('Название техкарты:',t.title||t.file_name||'Техкарта');if(title===null)return;title=title.trim();if(!title)return;rpc('manager_tech_card_update',{p_tech_card_id:t.id,p_title:title,p_file_name:title}).then(refreshTechCards).then(function(){techEnhance();}).then(function(){status('Техкарта изменена.');}).catch(function(e){console.error('[QRChick][edit-tech]',e);status(GENERIC_ERROR,true);});}
  function techEnhance(){var box=document.getElementById('techList');if(!box)return;box.querySelectorAll('[data-qrc-edit-tech]').forEach(function(b){if(b.__qrcBound)return;b.__qrcBound=true;b.onclick=function(e){e.stopPropagation();var t=(state().techCards||[]).find(function(x){return String(x.id)===String(b.dataset.qrcEditTech);});editTech(t);};});}
  function refreshActive(){var r=root(),s=state();if(!r||!s.baseLoaded)return;var a=r.querySelector('.qr-recipe-subnav [data-section].on'),n=a?a.dataset.section:'recipes';if(n==='recipes')recipeBlock();else if(n==='catalog')catalogBlock();else if(n==='ingredients')ingredientsBlock();else if(n==='tech'){refreshTechCards().then(function(){techEnhance();}).catch(function(e){console.error('[QRChick][tech-refresh]',e);status(GENERIC_ERROR,true);});}}
  function refreshRecipeProducts(){var r=root(),p=r&&r.querySelector('[data-section="recipes"] .qrchick-panel'),b=p&&p.querySelector('[data-qrc-product-list]');if(!b)return;var count=products().length;if(p.dataset.productCount===String(count))return;recipeBlock();}
  function boot(){photoUpload();refreshActive();var tries=0;(function waitData(){if(state().baseLoaded){refreshActive();return;}if(++tries<40)setTimeout(waitData,250);})();window.addEventListener('manager-venue-selected',function(){setTimeout(function(){refreshActive();},0);});window.addEventListener('qr-recipes-data-changed',function(){setTimeout(function(){refreshActive();refreshRecipeProducts();},0);});window.addEventListener('qr-recipes-view-updated',function(){setTimeout(function(){refreshActive();refreshRecipeProducts();},0);});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
