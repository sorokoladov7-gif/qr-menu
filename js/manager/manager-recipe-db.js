/* QR Menu — lazy recipe/ingredient catalog backed by Supabase. */
(function(){
  'use strict';
  if(window.__QR_RECIPE_DB__)return; window.__QR_RECIPE_DB__=true;
  var section='';
  function st(){return window.__QR_MANAGER_RECIPES_STATE__||{};}
  function norm(v){return String(v||'').toLowerCase().replace(/ё/g,'е').replace(/[^а-яa-z0-9]+/g,' ').trim();}
  function products(){return (st().products||[]).map(function(x){return x&&x.name;}).filter(Boolean).slice(0,300);}
  function currentProductName(){var s=st(),p=(s.products||[]).find(function(x){return x.id===s.selected;});return p&&p.name||'';}
  function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c];});}
  function activeSection(){var root=document.querySelector('.recipe-tab-container[data-qr-sections="1"]');if(!root)return section;var active=root.querySelector('.qr-recipe-subnav [data-section].on,.qr-recipe-subnav [data-section][aria-selected="true"]');return active?String(active.getAttribute('data-section')||''):section;}
  function rpc(name,args){if(!window.db||typeof db.rpc!=='function')return Promise.reject(new Error('База данных недоступна'));return db.rpc(name,args).then(function(r){if(r.error)throw r.error;return r.data||[];});}
  function recipeSearch(names,limit){var s=st();if(!s.venueId)return Promise.resolve([]);return rpc('manager_recipe_catalog_for_venue',{p_venue_id:s.venueId,p_product_names:names,p_limit:limit||80});}
  function ingredientSearch(names,limit){var s=st();if(!s.venueId)return Promise.resolve([]);return rpc('manager_ingredient_catalog_for_venue',{p_venue_id:s.venueId,p_product_names:names,p_limit:limit||160});}
  function searchQuery(query){var q=String(query||'').trim(),names=q?[q]:products();if(!names.length)return Promise.resolve([]);return recipeSearch(names,60).then(function(rows){return rows.map(function(r){var ing=Array.isArray(r.ingredients)?r.ingredients:[];return{title:r.recipe_name||'Рецептура',url:'',snippet:[r.category,r.cuisine,r.description,ing.slice(0,8).map(function(x){return x.name+' '+(x.quantity==null?'':x.quantity)+' '+(x.unit||'');}).join(', ')].filter(Boolean).join(' · '),source:'QRChick DB',recipe:r};});});}
  function setStatus(text,error){var e=document.getElementById('qrReceptAiStatus');if(e){e.textContent=text;e.style.color=error?'#fca5a5':'';}}
  function catalogRender(rows){var box=document.getElementById('catalogList');if(!box)return;var html='<div style="display:grid;gap:10px">';if(!rows.length){html+='<div class="muted">Для блюд текущего меню рецептуры в базе не найдены.</div>';}else{html+=rows.map(function(r){var ing=Array.isArray(r.ingredients)?r.ingredients:[];return '<div style="padding:12px;border:1px solid rgba(255,255,255,.08);border-radius:12px"><div style="display:flex;justify-content:space-between;gap:10px"><b>'+esc(r.recipe_name)+'</b><span class="muted">'+esc(r.matched_product_name||'')+'</span></div><div class="muted" style="font-size:11px;margin-top:4px">'+esc([r.category,r.cuisine,r.difficulty].filter(Boolean).join(' · '))+'</div><div style="font-size:12px;margin-top:8px">'+ing.slice(0,16).map(function(x){return esc(x.name)+' '+esc(x.quantity==null?'':x.quantity)+' '+esc(x.unit||'');}).join(' · ')+'</div></div>';}).join('');}html+='</div>';box.innerHTML=html;}
  function ingredientRender(rows){var box=document.getElementById('ingredientsDbList');if(!box)return;var html='<div style="display:grid;gap:6px">';if(!rows.length)html+='<div class="muted">Для блюд текущего меню ингредиенты в общей базе не найдены.</div>';else html+=rows.map(function(r){return '<div style="display:flex;justify-content:space-between;gap:10px;padding:8px 10px;border:1px solid rgba(255,255,255,.07);border-radius:9px"><span><b>'+esc(r.name)+'</b>'+ (r.matched_recipe_name?' <span class="muted">· '+esc(r.matched_recipe_name)+'</span>':'')+'</span><span class="muted">'+esc(r.unit||'')+'</span></div>';}).join('');html+='</div>';box.innerHTML=html;}
  function loadCatalog(){var names=products();if(!names.length){catalogRender([]);return;}setStatus('Qrchick загружает только рецептуры, относящиеся к меню этого заведения…',false);recipeSearch(names,100).then(function(rows){catalogRender(rows);setStatus('База блюд загружена выборочно из общей базы.',false);return rows;}).catch(function(e){console.error('[Qrchick Recipe DB]',e);catalogRender([]);setStatus('Ошибка системы. Обратитесь к администратору платформы.',true);});}
  function loadIngredients(){var names=products();if(!names.length){ingredientRender([]);return;}setStatus('Qrchick подбирает только необходимые ингредиенты для блюд меню…',false);ingredientSearch(names,180).then(function(rows){ingredientRender(rows);setStatus('Ингредиенты загружены выборочно из общей базы.',false);return rows;}).catch(function(e){console.error('[Qrchick Ingredient DB]',e);ingredientRender([]);setStatus('Ошибка системы. Обратитесь к администратору платформы.',true);});}
  function bind(){var root=document.querySelector('.recipe-tab-container[data-qr-sections="1"]');if(!root||root.__qrDbBound)return;if(root){root.__qrDbBound=true;root.addEventListener('click',function(e){var b=e.target.closest&&e.target.closest('.qr-recipe-subnav [data-section]');if(!b)return;section=String(b.getAttribute('data-section')||'');setTimeout(function(){if(section==='catalog')loadCatalog();if(section==='ingredients')loadIngredients();},40);});}}
  function patchFetch(){if(window.__QR_RECIPE_DB_FETCH__)return;var original=window.fetch;if(typeof original!=='function')return;window.__QR_RECIPE_DB_FETCH__=true;window.fetch=function(input,init){try{var url=typeof input==='string'?input:(input&&input.url)||'';if(!/\/api\/manager-ai(?:\?|$)/.test(url)||!init||String(init.method||'GET').toUpperCase()!=='POST')return original.apply(this,arguments);var body=JSON.parse(String(init.body||'{}'));if(body.feature!=='recipes')return original.apply(this,arguments);if(body.mode==='web_search'){return searchQuery(body.query||body.message).then(function(results){return new Response(JSON.stringify({ok:true,feature:'recipes',query:body.query||body.message,results:results}),{status:200,headers:{'Content-Type':'application/json'}});}).catch(function(){return new Response(JSON.stringify({ok:true,feature:'recipes',query:body.query||body.message,results:[]}),{status:200,headers:{'Content-Type':'application/json'}});});}
        /* Auto-analysis is database-backed only. Do not contact Gemini from Recipes. */
        var ctx={};try{ctx=typeof body.context==='string'?JSON.parse(body.context||'{}'):(body.context||{});}catch(_){ctx={};}
        var pname=ctx.product&&ctx.product.name||currentProductName();
        if(!pname||!['catalog','ingredients'].includes(activeSection()))return Promise.resolve(new Response(JSON.stringify({ok:true,feature:'recipes',answer:'Откройте блок «База блюд» или «Ингредиенты», чтобы Qrchick выполнил поиск по общей базе рецептур.',actions:[]}),{status:200,headers:{'Content-Type':'application/json'}}));
        return recipeSearch([pname],40).then(function(rows){var local=Array.isArray(ctx.ingredients&&ctx.ingredients.items)?ctx.ingredients.items:st().ingredients||[];var by=function(name){var n=norm(name),exact=local.find(function(x){return norm(x.name)===n;});if(exact)return exact;return local.find(function(x){var z=norm(x.name);return z&&n&&(z.indexOf(n)>=0||n.indexOf(z)>=0);});};var hit=rows[0],ings=hit&&Array.isArray(hit.ingredients)?hit.ingredients:[];var mapped=ings.map(function(x){var m=by(x.name);return m?{ingredient_id:m.id,quantity:Number(x.quantity)||0,note:'Общая база рецептур'}:null;}).filter(Boolean);return new Response(JSON.stringify({ok:true,feature:'recipes',answer:hit?'Найдена рецептура в общей базе. Проверьте состав перед сохранением.':'В общей базе подходящая рецептура не найдена.',actions:hit&&mapped.length?[{type:'save_recipe',title:'Применить найденную рецептуру',reason:'Источник: внутренняя база рецептур QR Menu.',payload:{product_id:ctx.product&&ctx.product.id||st().selected,rows:mapped}}]:[]}),{status:200,headers:{'Content-Type':'application/json'}});}).catch(function(){return new Response(JSON.stringify({ok:true,feature:'recipes',answer:'Ошибка системы. Обратитесь к администратору платформы.',actions:[]}),{status:200,headers:{'Content-Type':'application/json'}});});
      }catch(e){return original.apply(this,arguments);} };}
  function boot(){patchFetch();bind();setInterval(function(){bind();},1000);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();